# TODO

## Step 1
- Identify and fix syntax/React/Next.js errors causing build/runtime failures.

## Step 2
- Edit `app/page.js` to remove duplicate imports/duplicate component definitions and fix mismatched JSX structure.

## Step 3
- Edit `app/safaris/page.js` to fix invalid JSX in `PageHero` (duplicate props/children placement) and ensure required imports/data exist.

## Step 4
- Run `npm run build` (and/or `npm run dev`) to verify the errors are resolved.

